name: Lining and Testing
on:
  pull_request:

jobs:
  linters:
    runs-on: rockylinux/8
    steps:
        - name: Check out the repo
          uses: actions/checkout@v3

        - name: Install dependencies
          run: |
            dnf install -y python3-pip
            pip3 install --upgrade pip
            pip3 install poetry
            poetry export -f requirements.txt --output requirements.txt --without-hashes --with=dev
            pip3 install -r requirements.txt

        - name: Run linters
          run: |
            black --check .
            isort --check-only --diff .
            mypy --junit-xml TEST-mypy.xml .
            bandit -c pyproject.toml -r -f xml -o TEST-bandit.xml

        - name: Publish linter reports
          uses: mikepenz/action-junit-report@v4
          if: success() || failure() # always run even if the previous step fails
          with:
            report_paths: 'test_results/TEST-*.xml'


  tests:
    runs-on: rockylinux/8
    steps:
      - name: Check out the repo
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v3
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install poetry
          poetry export -f requirements.txt --output requirements.txt --without-hashes --with=dev
          pip install -r requirements.txt

      - name: Run tests
        run: pytest --junitxml=test_results/TEST-pytest.xml

      - name: Publish test report
        uses: mikepenz/action-junit-report@v4
        if: success() || failure() # always run even if the previous step fails
        with:
          report_paths: 'test_results/TEST-*.xml'
