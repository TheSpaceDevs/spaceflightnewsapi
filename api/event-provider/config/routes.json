{
  "routes": [
    {
      "method": "GET",
      "path": "/event-providers",
      "handler": "event-provider.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/event-providers/count",
      "handler": "event-provider.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/event-providers/:id",
      "handler": "event-provider.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/event-providers",
      "handler": "event-provider.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/event-providers/:id",
      "handler": "event-provider.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/event-providers/:id",
      "handler": "event-provider.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
